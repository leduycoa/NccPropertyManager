export enum Userstatus {
  active = 'ACTIVE',
  deleted = 'DELETED',
  inactive = 'INACTIVE',
}

export enum UserType {
  user = 'USER',
  agent = 'AGENT',
  admin = 'ADMIN',
  supplier = 'SUPLLIER',
  other = 'OTHER',
}
